# 给下一个 AI 的交接说明

下面这段话可以直接发给下一个 AI：

---

请接手 `/Users/goudan/Github/codex-feishu`。

这是一个单机版 `codex-feishu`，目标是把 Codex 的工作流接到飞书里。当前状态不是从零开始，而是已经有可运行的 MVP。

你先阅读这些文件：

- `docs/README.md`
- `docs/09-当前实现状态与待办.md`
- `src/app/command-service.ts`
- `src/app/feishu-bot-runtime.ts`
- `src/app/feishu-message-service.ts`
- `src/feishu/client.ts`
- `src/codex/app-server-client.ts`
- `src/workers/codex/codex-worker.ts`
- `src/db/schema.ts`

当前已经确认：

- `npm run check` 通过
- `npm test` 通过
- 测试数 `23/23`
- `npm run dev` 能启动
- 飞书长连接能连上
- 普通消息已能回复
- `/bind /绝对路径` 已支持
- 群聊话题上下文和 `reply_in_thread` 已接入

当前主要未完善方向：

1. 审批流还没完整实现
2. 卡片 UI 和按钮交互还比较基础
3. `/compact` 当前受本机 `codex app-server` 版本限制，会优雅失败
4. 仓库还没做首次 git commit 整理

注意几个已知坑：

- `dotenv` 必须 `override: true`，否则会读到 shell 里的旧飞书配置
- Feishu Node SDK 的 `im.message.receive_v1` 是扁平结构，不要再按 `data.event.message` 处理
- 群聊默认有 `@机器人` 门禁逻辑
- `.env` 有本地真实飞书配置，但不要提交

请先不要重写架构，也不要把现有能跑的链路推翻。优先在当前基础上继续补功能。

建议你先做：

1. 完整审批流
2. 更完整的卡片交互
3. `/compact` 的能力探测和兼容

---

## 交接目标

这份交接说明的目标不是让下一个 AI“重新理解项目”，而是让它在当前可运行状态上继续推进，而不是重复排查已经解决的问题。

